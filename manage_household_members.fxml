<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.example.demo4.controllers.ManageHouseholdMembersController"
            style="-fx-background-color: #f5f7fa;">

    <!-- ===== TOP ===== -->
    <top>
        <VBox spacing="0">
            <padding>
                <Insets top="25" right="30" bottom="10" left="30"/>
            </padding>

            <HBox alignment="CENTER_LEFT" spacing="10">
                <Label text="👥" style="-fx-font-size: 24px;"/>
                <Label text="Quản Lý Thành Viên Hộ Khẩu"
                       style="-fx-font-size: 22px;
                              -fx-font-weight: bold;
                              -fx-text-fill: #2c3e50;"/>
            </HBox>

            <Separator/>
        </VBox>
    </top>

    <!-- ===== CENTER ===== -->
    <center>
        <HBox spacing="15" style="-fx-padding: 20 30 20 30;">

            <!-- ===== MEMBERS IN HOUSE ===== -->
            <VBox spacing="8" VBox.vgrow="ALWAYS" HBox.hgrow="ALWAYS">

                <Label text="Thành Viên Trong Hộ"
                       style="-fx-font-size: 16px;
                              -fx-font-weight: bold;"/>

                <ScrollPane fitToWidth="true" fitToHeight="true">
                    <TableView fx:id="tableMembers"
                               prefHeight="400">

                        <columns>
                            <TableColumn fx:id="colName1"
                                         text="Họ Tên"
                                         prefWidth="220"/>
                            <TableColumn fx:id="colRelation1"
                                         text="Quan Hệ"
                                         prefWidth="160"/>
                        </columns>

                    </TableView>
                </ScrollPane>

                <HBox spacing="8">
                    <Button text="➖ Gỡ Khỏi Hộ"
                            onAction="#removeMember"
                            prefWidth="130"/>

                    <TextField fx:id="tfTargetHousehold"
                               promptText="ID hộ khác"
                               prefWidth="120"/>

                    <Button text="🔄 Chuyển Hộ"
                            onAction="#moveToOtherHousehold"
                            prefWidth="120"/>
                </HBox>

            </VBox>

            <!-- ===== NO HOUSE ===== -->
            <VBox spacing="8" VBox.vgrow="ALWAYS" HBox.hgrow="ALWAYS">

                <Label text="Công Dân Chưa Có Hộ"
                       style="-fx-font-size: 16px;
                              -fx-font-weight: bold;"/>

                <ScrollPane fitToWidth="true" fitToHeight="true">
                    <TableView fx:id="tableNoHouse"
                               prefHeight="400">

                        <columns>
                            <TableColumn fx:id="colName2"
                                         text="Họ Tên"
                                         prefWidth="220"/>
                            <TableColumn fx:id="colRelation2"
                                         text="Quan Hệ"
                                         prefWidth="160"/>
                        </columns>

                    </TableView>
                </ScrollPane>

                <Button text="➕ Thêm Vào Hộ"
                        onAction="#addMember"
                        prefWidth="150"/>
            </VBox>

        </HBox>
    </center>

    <!-- ===== BOTTOM ===== -->
    <bottom>
        <HBox alignment="CENTER" style="-fx-padding: 15 30 25 30;">
            <Button text="✕ Đóng"
                    onAction="#close"
                    prefWidth="120"/>
        </HBox>
    </bottom>

</BorderPane>
