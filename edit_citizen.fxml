<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.Insets?>

<VBox xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.example.demo4.controllers.EditCitizenController"
      spacing="0"
      style="-fx-background-color: #f5f7fa;">

    <padding>
        <Insets top="25" right="30" bottom="25" left="30"/>
    </padding>

    <!-- Header -->
    <HBox spacing="10" alignment="CENTER_LEFT" style="-fx-padding: 0 0 20 0;">
        <Label text="✏" style="-fx-font-size: 24px;"/>
        <Label text="Sửa Thông Tin Công Dân"
               style="-fx-font-size: 22px;
                      -fx-font-weight: bold;
                      -fx-text-fill: #2c3e50;"/>
    </HBox>

    <Separator style="-fx-background-color: #e0e6ed; -fx-padding: 0 0 20 0;"/>

    <!-- FORM -->
    <ScrollPane fitToWidth="true" style="-fx-background-color: transparent;">
        <VBox spacing="16" style="-fx-padding: 0 5 0 0;">

            <!-- Họ tên -->
            <VBox spacing="6">
                <Label text="Họ và tên *"/>
                <TextField fx:id="tfFullName"/>
            </VBox>

            <!-- Tên gọi khác -->
            <VBox spacing="6">
                <Label text="Tên gọi khác"/>
                <TextField fx:id="tfAlias"/>
            </VBox>

            <!-- Ngày sinh -->
            <VBox spacing="6">
                <Label text="Ngày sinh *"/>
                <DatePicker fx:id="dpDob"/>
            </VBox>

            <!-- Nơi sinh -->
            <VBox spacing="6">
                <Label text="Nơi sinh"/>
                <TextField fx:id="tfPlaceOfBirth"/>
            </VBox>

            <!-- Quê quán -->
            <VBox spacing="6">
                <Label text="Quê quán"/>
                <TextField fx:id="tfHometown"/>
            </VBox>

            <!-- Dân tộc -->
            <VBox spacing="6">
                <Label text="Dân tộc"/>
                <TextField fx:id="tfEthnicity"/>
            </VBox>

            <Separator/>

            <!-- CCCD -->
            <VBox spacing="6">
                <Label text="Số CCCD *"/>
                <TextField fx:id="tfCccd"/>
            </VBox>

            <!-- Ngày cấp CCCD -->
            <VBox spacing="6">
                <Label text="Ngày cấp CCCD"/>
                <DatePicker fx:id="dpCccdIssueDate"/>
            </VBox>

            <!-- Nơi cấp CCCD -->
            <VBox spacing="6">
                <Label text="Nơi cấp CCCD"/>
                <TextField fx:id="tfCccdIssuePlace"/>
            </VBox>

            <Separator/>

            <!-- Nghề nghiệp -->
            <VBox spacing="6">
                <Label text="Nghề nghiệp *"/>
                <TextField fx:id="tfJob"/>
            </VBox>

            <!-- Nơi làm việc -->
            <VBox spacing="6">
                <Label text="Nơi làm việc"/>
                <TextField fx:id="tfWorkplace"/>
            </VBox>

            <!-- Địa chỉ trước đây -->
            <VBox spacing="6">
                <Label text="Địa chỉ trước đây"/>
                <TextField fx:id="tfPreviousAddress"/>
            </VBox>

            <!-- Ngày đăng ký -->
            <VBox spacing="6">
                <Label text="Ngày đăng ký"/>
                <DatePicker fx:id="dpRegisterDate"/>
            </VBox>

            <Separator/>

            <!-- Chủ hộ -->
            <CheckBox fx:id="cbHouseholder" text="Là chủ hộ"/>

            <!-- Quan hệ -->
            <VBox spacing="6">
                <Label text="Quan hệ với chủ hộ"/>
                <TextField fx:id="tfRelation"/>
            </VBox>

            <!-- Message -->
            <Label fx:id="lblMessage"
                   style="-fx-text-fill: #e74c3c;
                          -fx-font-size: 12px;
                          -fx-font-weight: 600;"/>

        </VBox>
    </ScrollPane>

    <!-- Buttons -->
    <HBox spacing="12" alignment="CENTER" style="-fx-padding: 20 0 0 0;">
        <Button text="💾 Lưu"
                onAction="#handleSave"
                prefWidth="150"
                style="-fx-background-color: #27ae60;
                       -fx-text-fill: white;
                       -fx-font-weight: bold;"/>

        <Button text="✕ Hủy"
                onAction="#handleCancel"
                prefWidth="120"
                style="-fx-background-color: #ecf0f1;
                       -fx-text-fill: #7f8c8d;"/>
    </HBox>

</VBox>
